version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/app

hooks:
  ApplicationStop:
    - location: stop.sh
      timeout: 300
      runas: ubuntu

  BeforeInstall:
    - location: start.sh
      timeout: 300
      runas: ubuntu
